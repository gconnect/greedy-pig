{
  "name": "my-dapp",
  "version": "0.1.0",
  "description": "JS DApp",
  "dependencies": {
    "cartesi-router": "^1.0.4",
    "cartesi-wallet": "^1.0.5",
    "dotenv": "^16.4.4",
    "ethers": "^6.7.1",
    "uuid": "^9.0.1",
    "viem": "^2.7.3"
  },
  "devDependencies": {
    "esbuild": "^0.19.2"
  },
  "scripts": {
    "build": "yarn && docker run --privileged --rm tonistiigi/binfmt:riscv && npm i -g @sunodo/cli  && sunodo build",
    "start:dev": "ROLLUP_HTTP_SERVER_URL=\"http://127.0.0.1:5004\" node src/index.js",
    "start": "sunodo run"
  },
  "keywords": [
    "cartesi"
  ],
  "license": "MIT"
}
